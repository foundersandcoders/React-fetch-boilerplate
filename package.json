{
  "name": "wheeloffinalfate",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.2.0",
    "react-dom": "^16.2.0",
    "react-scripts": "1.1.0"
  },
  "scripts": {
    "start":
  "npm run api & if-env NODE_ENV=production && npm run react || npm run devstart-react && fg",
    "react": "react-scripts start",
    "devstart-react": "react-scripts start",
    "api" : "node server/start.js",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  },
  "proxy" : "http://localhost:8000"
}
